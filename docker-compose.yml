version: '3'

services:
  data:
    image: json-api-server
    build: './data'
    ports:
      - 3001:3000
    container_name: json-api-server

  tamboon:
    image: tamboon
    build: './'
    ports:
      - 80:80
    container_name: tamboon